/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    // reactStrictMode: true,
    // images: {
    //     unoptimized: true, // Ensures static exports work properly
    //   },
    // trailingSlash: true, // Helps with static hosting issues
};

export default nextConfig;
